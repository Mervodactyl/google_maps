var mapOptions = {
  center: new google.maps.LatLng(51.563296,-0.1074350000000095),
  zoom: 13,
  mapTypeId: google.maps.MapTypeId.ROADMAP
};

new google.maps.Map(document.getElementById("map"), mapOptions);
